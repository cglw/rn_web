import {
  Badge as badge,
  Carousel as cs,
  Grid as grid,
  Tabs as tab,
} from 'antd-mobile';
export const Badge = badge;
export const Carousel = cs;
export const Grid = grid;
export const Tabs = tab;
